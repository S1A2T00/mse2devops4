services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: api-container
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
